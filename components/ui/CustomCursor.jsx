'use client';

/**
 * HireLabs custom cursor
 *
 * The custom cursor has been intentionally disabled.
 * The website now uses the browser's native cursor for
 * a cleaner, more natural interface.
 */
export function CustomCursor() {
  return null;
}