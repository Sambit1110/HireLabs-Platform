# Legacy static prototype

`index.html`, `signup.html`, `app.js`, `style.css`, and `supabase.js` at the repository root are the original standalone prototype.

The production application is the Next.js App Router implementation in `app/`, `components/`, and `lib/`. New work should be added there. The static prototype is intentionally retained for reference and should not be imported by the Next.js application.
